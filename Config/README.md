# Local configuration

This directory intentionally contains no credentials. Runtime secrets must be stored by
`SecurityKit` in the system Keychain. Never add API keys, authenticated URLs, server tokens,
or exported Keychain values to this repository.
